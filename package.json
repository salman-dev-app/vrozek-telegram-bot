{
  "name": "vrozek-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "VROZEK AI — intelligent Telegram ecosystem (bot, group assistant, products, knowledge, moderation, dashboard) for Cloudflare Workers",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "setup": "node scripts/setup.js"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
